# Spring App configuration
spring.application.name=Stall-Reservation

# Secure Database Connection (configured via environment variables)
spring.datasource.url=jdbc:mysql://localhost:3306/bookfair?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Config
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# OIDC Identity Provider Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=${OIDC_ISSUER}
app.security.oauth2.audience=${OIDC_AUDIENCE}

# CORS Allowed Origins
app.security.cors.allowed-origins=https://localhost:5173

# SSL/HTTPS Config (configured via environment variables)
server.port=8443
server.ssl.key-store=classpath:keystore.p12
server.ssl.key-store-type=PKCS12
server.ssl.key-alias=bookfair
server.ssl.key-store-password=${SSL_KEYSTORE_PASSWORD}

# Mail Configuration (configured via environment variables)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.debug=true
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
